#include <stdio.h>
/**
 * main -use printf instead of puts to print a code
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
